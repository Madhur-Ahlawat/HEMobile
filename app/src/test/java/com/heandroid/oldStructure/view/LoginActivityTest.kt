package com.heandroid.oldStructure.view

class LoginActivityTest {

    val loginCredentialsTest = LoginActivityTest()

  /*  @Test
    fun emailValidator_CorrectEmailSimple_ReturnsTrue() {
        assertTrue(LoginActivity.isUserNameNotnull("johnsmith32"))
    }

    @Test
    fun userName_NulluserName_ReturnsFalse() {
        assertFalse(LoginActivity.isUserNameNotnull(null))
    }

    @Test
    fun userPassword_NulluserPassword_ReturnsFalse() {
        assertFalse(LoginActivity.isUserNameNotnull(null))
    }*/

    /*   @Test
       fun login_Credentials(correctLogin: String, correctPassword: String) {
           //given

           val correctLogin = "johnsmith32"
           val correctPassword = "Welcome1!"
           //when
           val result = loginCredentialsTest.login_Credentials(correctLogin, correctPassword)
           //val resultoo = loginCredentialsTest.`login with correct login and password`(correctLogin,
               //correctPassword)
           //then
          // result.test().assertResult(true)
       }*/

    /*  @Test
      fun `do not login with only correct login`() {
          //given
          val login = "johnsmith32"
          val password = "anyPassword"
          //when
          val result = objectUnderTest.login(login, password)
          //then
          result.test().await()
              .assertResult(false)
      }*/

    /*  @Test
      fun `do not login with only correct password`() {
          //given
          val login = "anyLogin"
          val password = "Welcome1!"
          //when
          val result = objectUnderTest.login(login, password)
          //then
          result.test().await()
              .assertResult(false)
      }*/
}